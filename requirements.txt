# Core Rasa - pinned versions for stability
rasa==3.6.0
rasa-sdk==3.6.0

# Database
psycopg2-binary==2.9.7

# HTTP requests
requests==2.31.0

# Environment management
python-dotenv==1.0.0

# Only essential packages for Railway deployment